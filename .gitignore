# Secrets — never commit
.env
*.pem
*.key
.credentials.json
hosts.yml
gh-hosts.yml
device.key
*-credentials.json
6c4cfcc1-*.json

# Runtime state / data / logs
state/
data/*
!data/cpp-aibom-components.json
logs/
audit.db
audit.db-journal
audit.db-wal
audit.db-shm
*.sqlite
*.sqlite-journal
last-poll.json
.last-scrub

# Galileo eval workspace — generated datasets may embed issue/PR text; keep the
# committed config but never commit generated cases or local packets.
.galileo/datasets/
.galileo/packets/
.claude/skills/eval-*/

# OS / editor junk
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/

# Backups created in-place
*.bak
*.bak-*
*.pre-db-bak
